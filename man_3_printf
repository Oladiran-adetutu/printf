PROJECT NAME
_printf - prints out the data and format
PROJECT SYNOPSIS
1. printf format [args]...
PROJECT DESCRIPTION
It prints out arg(s) based on a format

If format contains format specifiers, the subsequent arguments are formatted and placed into the resulting string in place of the appropriate specifiers.

_printf will take an uncountable of arguments.

Scenario
_printf("%s", word);

This prints out the value of the word with %s and gives the format of said word.

2. The conversation specified

IP % c - prints a character.
IP % s - prints the string.
IP %% - prints % and prints the decimal. 
IP % i - prints the integer number.
IP % b - prints the binary rep.
IP % p - prints the pointer.
IP % o - prints the octal representation.
IP % r - prints the string in reverse.
IP % u - prints an unsigned integer.
IP % x - prints the hexadecimal rep.


Created a repo named printf - https://github.com/Oladiran-adetutu/printf
THE  AUTHORS OF THIS PROJECT
Oladiran-adetutu https://github.com/Oladiran-adetutu
Black-Sparkles https://github.com/Black-Sparkles
